#!/usr/bin/env bash
# MultiManager .app launcher (uv venv)
DIR="$(cd "$(dirname "$0")/../../.." && pwd)"
VENV="$DIR/.venv"
if [ -f "$VENV/bin/activate" ]; then
    source "$VENV/bin/activate"
else
    export PYTHONPATH="$DIR:$PYTHONPATH"
fi
cd "$DIR" && exec python3 app.py
